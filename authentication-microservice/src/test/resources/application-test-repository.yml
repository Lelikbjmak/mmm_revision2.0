# ----- PROPERTIES -----
#spring:
#  output:
#    ansi:
#      enabled: ALWAYS
  jpa:
    hibernate:
      ddl-auto: none
    database-platform: org.hibernate.dialect.H2Dialect
  datasource:
    platform: h2
    url: jdbc:h2:mem:mmm_rev2
    driverClassName: org.h2.Driver
    username: user
    password: user
  h2:
    console:
      enabled: true
  application:
    name: user-repository-test

logging:
  level:
    org:
      hibernate:
        sql: info

user:
  username: testUser
  password: 1111
  mail: test@gmail.com
